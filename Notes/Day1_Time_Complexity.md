# Day 1 – Time Complexity

O(1) → Constant Time
Example: Accessing array element by index

O(n) → Linear Time
Example: Single loop

O(n²) → Quadratic Time
Example: Nested loops

Key Rules:
- If shifting happens → O(n)
- Direct access → O(1)
- Nested loops → O(n²)
